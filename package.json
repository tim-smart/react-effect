{
  "name": "react-effect",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "react-effect:clean": "cd $INIT_CWD && rm -rf dist/ *.tsbuildinfo",
    "react-effect:tsc": "cd $INIT_CWD && tsc"
  },
  "devDependencies": {
    "@tsplus/installer": "^0.0.153",
    "@types/node": "^18.11.9",
    "lerna": "^6.0.3",
    "prettier": "^2.7.1",
    "tsconfig-paths": "^4.1.0",
    "typescript": "^4.9.3"
  }
}
